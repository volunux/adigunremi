[ upload.fields([{ name: 'photo', maxCount: 4 }]) , (req , res) => {
															
																																																														console.log(req.body);
																																																																										console.log(req.files)
	}],