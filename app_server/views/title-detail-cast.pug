extends layout

include include/tmix

block content

	.title-body-actor
		ul
			for actor in cast
				li.mr-4
					a(href ='/actor/' + actor.url)
						actor.cover_image
							+fixDir(actor.cover_image.path)
					p
						a(href ='/actor/' + actor.url) #{actor.name}