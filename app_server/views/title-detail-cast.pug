extends layout

include include/tmix

block content

	.title-body-actor
		ul
			for actor in cast
				li.mr-4
					a(href ='/actor/' + actor.url)
						actor.cover_image
							+fixDirTt(actor.cover_image.location)
					p
						a(href ='/actor/' + actor.url) #{actor.name}